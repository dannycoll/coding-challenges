export type drawFn = (
  context: CanvasRenderingContext2D,
  frameCount: number
) => void;

export type CanvasOptions = {
  context?: string;
  width?: number;
  height?: number;
  background?: string;
};




























for i in Range(10):
  print(i)



for (var i = 0; i < 10; i = i+1) {
  console.log(i);
}
